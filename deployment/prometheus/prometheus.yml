global:
  scrape_interval: 5s

scrape_configs:
  - job_name: 'prometheus'
    static_configs:
      - targets: ['localhost:9090']

  # placeholder: scrape application metrics endpoints (add your services here)
  - job_name: 'kafka-producer'
    static_configs:
      - targets: ['producer:8000']
