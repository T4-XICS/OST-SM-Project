apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: influxdb-data
  namespace: ics
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 5Gi
---
apiVersion: v1
kind: Service
metadata:
  name: influxdb
  namespace: ics
spec:
  selector:
    app: influxdb
  ports:
    - name: http
      port: 8086
      targetPort: 8086
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: influxdb
  namespace: ics
  labels:
    app: influxdb
spec:
  replicas: 1
  selector:
    matchLabels:
      app: influxdb
  template:
    metadata:
      labels:
        app: influxdb
    spec:
      containers:
        - name: influxdb
          image: influxdb:latest
          imagePullPolicy: IfNotPresent
          env:
            - name: INFLUXDB_INIT_MODE
              value: setup
            - name: INFLUXDB_INIT_USERNAME
              value: admin
            - name: INFLUXDB_INIT_PASSWORD
              value: adminpass
            - name: INFLUXDB_INIT_ORG
              value: ucs
            - name: INFLUXDB_INIT_BUCKET
              value: swat_db
            - name: INFLUXDB_INIT_ADMIN_TOKEN
              value: QxzNbxYC3mh15AOYK5e1rweiLzHsS9WqlCDvZ9VTRdTcvVO3gbp2FexttgNzwecf06BdA5On8SSPu6Ky1UBDXQ==
          ports:
            - containerPort: 8086
          volumeMounts:
            - name: data
              mountPath: /var/lib/influxdb2
      volumes:
        - name: data
          persistentVolumeClaim:
            claimName: influxdb-data
